CREATE DATABASE cafe_pos;
USE cafe_pos;

CREATE TABLE login (
id INT AUTO_INCREMENT PRIMARY KEY,
username VARCHAR(15) UNIQUE,
pin_hash TEXT, 
role VARCHAR(15),
created_at TIMESTAMP
);

CREATE TABLE categories (
id INT PRIMARY AUTO_INCREMENT,
category VARCHAR(30)
);

CREATE TABLE product (
id INT PRIMARY KEY AUTO_INCREMENT,
category_id INT,
name VARCHAR(67),
price DECIMAL(10, 2),
stock INT,
low_stock_threshold INT,
created_at TIMESTAMP,
FOREIGN KEY (category_id) REFERENCES categories (id)
);

CREATE TABLE transactions (
id INT PRIMARY KEY AUTO_INCREMENT,
user_id INT,
total_amount DECIMAL(10,2),
created_at TIMESTAMP,
FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE transaction_items (
id INT PRIMARY KEY AUTO_INCREMENT,
transaction_id INT,
product_id INT,
quantity INT,
price_at_sale DECIMAL(10,2),
FOREIGN KEY (transaction_id) REFERENCES transactions(id),
FOREIGN KEY (product_id) REFERENCES products(id)
);

-- insert data

INSERT INTO categories(category) VALUES('Tea');
INSERT INTO categories(category) VALUES('Pasta');
INSERT INTO categories(category) VALUES('Coffee');
INSERT INTO categories(category) VALUES('Pastries');

